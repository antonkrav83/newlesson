# Создали рандомный список от 0 до 1000

a = [64, 25, 12, 22, 11, 1, 2, 44, 3, 122, 23, 34, 908, 407]

# Сортируем список от минимальной цифры к большей.

for i in range(len(a)):
    for j in range(i + 1, len(a)):
        if a[i] > a[j]:
           a[i], a[j] = a[j], a[i]
print(a)

# Сортируем четные и не четные цифры. 
    
q = [i for i in a if i % 2 == 0] 
print(q)

z = [i for i in a if i % 2 != 0]      
print(z)  

# Считаем все списки.

print(len(a))
print(len(q))
print(len(z))